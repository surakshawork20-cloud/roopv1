# Supabase → Project Settings → API

# Public URL and anon key (safe to expose client-side)
NEXT_PUBLIC_SUPABASE_URL="https://xxxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOi..."

# Service role key — KEEP SECRET. Only used server-side (seed endpoint bypasses RLS).
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOi..."

# Gate for the POST /api/seed endpoint (any random string)
SEED_SECRET="change-this"
